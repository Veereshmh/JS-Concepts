// arrays

const myArr = [0,1,2,3,4,5]

const myHeros = ['Veeresh', 'Xyz']

const myArr2 = new  Array(1,2,3,4)
console.log(myArr[1]);

// Array Methods

myArr.push(6) // Add the Element in the Array
myArr.pop() // Remove the last Element in tha array

console.log(myArr);
